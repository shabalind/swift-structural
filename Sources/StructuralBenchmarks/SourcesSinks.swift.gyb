import StructuralCore
import StructuralExamples

% for N in range(1, 17):
var p${N}_2 = Point${N}(
    % for i in range(1, N + 1):
    _${i}: ${i}
    % if i != N:
    ,
    % end
    % end
)
var p${N}_1 = Point${N}(
    % for i in range(1, N + 1):
    _${i}: ${i} + 1
    % if i != N:
    ,
    % end
    % end
)
% end 

var boolSink = false
% for N in range(1, 17):
var point${N}Sink = p${N}_1
% end
var stringSink = ""
var intSink: Int = 0
var anySink: Any? = nil
